Bootstrap checklist

[x] Git initialized
[x] Folder structure created
[x] .gitignore created
[x] CMakeLists.txt created
[x] main.cpp created
[x] CMake configure succeeds
[x] Build succeeds
[x] Executable runs
[x] Logging system exists
    Implementation Steps:
    [x] Plan the Structure (create logger.h in tools/)
    [x] Define Log Levels (enum with INFO, WARN, ERROR)
    [x] Create the Log Function (takes level and message)
    [x] Add Timestamps (optional, using chrono)
    [x] Handle Output and Safety (cout/cerr, no crashes)
    [x] Test and Integrate (calls in main.cpp)
    [x] Add output to a file
    [x] Optimize the synchronous logger (readability + runtime + fewer temporary allocations)
    Testing:
    [x] Log function compiles without errors
    [x] INFO level outputs to console
    [x] WARN level outputs to console  
    [x] ERROR level outputs to stderr
    [x] Messages include level prefix (e.g., [INFO])
    [x] Optional: Timestamps included in output
    [x] No crashes when logging
    [x] Logging to files works correctly
    [x] Verify logger runs correctly through VS Code build/run flow
    Next Iteration: Async Logger Refactor
    [x] Move logger files into tools/logger/logger.h and tools/logger/logger.cpp
    [x] Update includes to use logger/logger.h
    [x] Keep logger.h as a thin API-only header
    [x] Move file/time/queue/thread internals into logger.cpp
    [x] Update CMake to compile logger.cpp
    [x] Add FATAL severity
    [x] Add a FATAl pop up window.
    [x] Add seperate DBWIN debug output function
    [x] Add startup-only init(mode, level)
    [x] Add shutdown() to flush and stop the logger worker thread
    [x] Add flush() so queued messages can be forced out on demand
    [x] Add another [] in the debug message, that says where it comes from. like Main, Engine, Vido Engine etc.
    [x] Make Logger compile as a dll.
    [ ] Add LogEntry queue with owned message strings
    [ ] Add one logger worker thread
    [ ] Add mutex + condition_variable for queue synchronization
    [ ] Make log() enqueue work instead of writing directly
    [ ] Keep OutputMode fixed after init() in async v1
    [ ] Allow LogLevel changes at runtime
    [ ] File-open failure falls back cleanly to console output
    [ ] Verify queued logs flush correctly on shutdown
    [ ] Verify logger works correctly when multiple threads log at once
    [ ] Optimize
[ ] Assertions/debug checks exist
[ ] Input handling exists
