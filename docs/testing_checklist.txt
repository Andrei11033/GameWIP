0. Bootstrap checklist

[x] Git initialized
[x] Folder structure created
[x] .gitignore created
[x] CMakeLists.txt created
[x] main.cpp created
[x] CMake configure succeeds
[x] Build succeeds
[x] Executable runs
[x] Logging system exists
[x] Logger implementation updated to current async DLL version
    Planned logger feature set:
    [x] Logger moved to tools/logger with logger.h, core/logger.cpp, and platform/win32/win32_logger.cpp
    [x] logger.h kept as a thin API-only header
    [x] logger core owns file/time/queue/thread internals
    [x] Logger builds as a DLL
    [x] CMake copies required runtime DLLs next to GameWIP.exe automatically
    [x] Log levels include INFO, WARN, ERR, and FATAL
    [x] Fatal popup window exists
    [x] Separate platform debug output function exists
    [x] Source tag is included in messages, e.g. [Main], [Engine]
    [x] Timestamps are included in log output
    [x] Time formatting uses thread-safe localtime_s
    [x] Time formatting uses strftime with a fixed stack buffer
    [x] init(mode, level, maxQueueSize) is startup-only configuration
    [x] OutputMode stays fixed after init()
    [x] LogLevel stays fixed after init()
    [x] Logger owns queued strings through LogEntry
    [x] Oversized log messages are truncated before entering the queue
    [x] Queued LogEntry objects are moved into the queue to avoid an extra copy
    [x] One logger worker thread processes queued messages
    [x] mutex + condition_variable protect the queue and worker coordination
    [x] log() enqueues instead of writing directly
    [x] flush() waits until the queue is empty and the worker is idle
    [x] shutdown() drains work and stops the worker thread cleanly
    [x] Automatic cleanup is registered with atexit
    [x] File-open or filesystem setup failure falls back to console output
    [x] WARN / ERR / FATAL always emit ANSI color codes on console output
    [x] Queue soft limit drops INFO/WARN when full
    [x] Queue hard limit drops all severities at 2x maxQueueSize
    [x] Dropped log count is exposed through getDroppedLogCount()
    [x] Logger init returns an explicit Result
    [x] Logger exposes last result and last platform error
    [x] Logger internals are grouped into one internal LoggerState object
    [x] Logger platform debug output and fatal popup calls go through an internal platform bridge
    [x] Normal log/debug-output timestamps are cached per second
    [x] Logger worker drains queued messages in batches
    [x] Hot log-line construction uses append/reserve instead of std::format
    [x] log() skips disabled and below-minimum messages before queueing
    [x] Duplicate init() calls are reported and ignored while the logger is running
    [x] Public logger header includes the standard headers it uses
    [x] Public logger API has /// documentation comments
    Verification:
    [x] Logger compiles without errors
    [x] INFO level outputs to console
    [x] WARN level outputs to console
    [x] ERROR level outputs to stderr
    [x] FATAL level outputs to stderr
    [x] Messages include [time][level][source]: message format
    [x] Platform debug output works in a Windows debug viewer
    [x] Logging to files works correctly
    [x] File-open failure falls back cleanly to console output
    [x] Logger runs correctly through the VS Code build/run flow
    [x] Queued logs flush correctly on shutdown
    [x] Logger works correctly when multiple threads log at once
    [x] Queue pressure test drops low-priority logs and preserves ERR markers
    [x] Small-queue pressure test exits normally without deadlocking
    [x] Logger hot path optimized with move enqueue + fixed-buffer time formatting
    Optional future polish:
    [x] Cache formatted timestamp strings by second if profiling shows time formatting cost matters
    [ ] Decide whether logs should always go beside the executable or under the project root
    [x] Consider wrapping logger globals into one internal LoggerState object
[x] Assertions/debug checks exist
    Planned assertion/debug feature set:
    [x] Debug checks live under tools/debug/assert with assert.h, core/assert.cpp, and platform/win32/win32_assert.cpp
    [x] Debug checks build as a small static Debug library
    [x] Debug library links against Logger for reporting failures
    [x] Logger does not depend on Debug, keeping dependency direction one-way
    [x] GAMEWIP_ASSERT(condition) macro exists
    [x] GAMEWIP_ASSERT_MSG(condition, message) macro exists
    [x] GAMEWIP_CHECK(condition) macro exists
    [x] GAMEWIP_CHECK_MSG(condition, message) macro exists
    [x] Assertion macros capture the failed condition text
    [x] Assertion macros capture file, line, and function
    [x] Recoverable check macros capture condition, file, line, and function
    [x] Assertion implementation formats a clear failure message
    [x] Assertion failures log through Logger
    [x] Assertion failures also emit platform debug output
    [x] Assertion failures flush logs before breaking
    [x] Recoverable check failures log through Logger
    [x] Recoverable check failures emit platform debug output and flush without breaking
    [x] Debug builds trigger a debugger break on assertion failure
    [x] Release builds compile assertions out when NDEBUG is defined
    [x] Assert/check always-on and always-off compile switches exist
    [x] Contradictory assert/check compile switches fail CMake configure
    [x] Passing assertions have no side effects
    [x] Assertion macros are safe to use in if/else statements
    [x] Public debug header includes only the standard headers it needs
    [x] Public debug API has /// documentation comments
    Verification:
    [x] Assert library compiles without errors
    [x] GameWIP links against Assert successfully
    [x] Passing GAMEWIP_ASSERT does nothing visible
    [x] Failing GAMEWIP_ASSERT logs the condition text
    [x] Failing GAMEWIP_ASSERT_MSG logs the custom message
    [x] Failure output includes file, line, and function
    [x] Failure output appears in console/log file according to Logger mode
    [x] Failure output appears in a Windows debug viewer
    [x] Debugger break triggers in a debug build
    [x] Release build compiles with assertions disabled
    [x] Assertion test code is removed or guarded after verification
    Optional future polish:
    [x] Add a recoverable debug-check macro if needed
    [ ] Add fatal assertion popup only if assertion failures need user-visible reporting
    [x] Add compile-time switches for always-on or always-off assertions
[x] Input handling exists
    Current raw input-state foundation:
    [x] Input module lives under engine/input/input.h and engine/input/input.cpp
    [x] Input builds as a small static Input library
    [x] First input milestone has no dependency on windowing/rendering code
    [x] Generic InputControl identifiers exist for keyboard, mouse, and gamepad controls
    [x] Current button state is stored by InputControl
    [x] Previous button state is stored by InputControl
    [x] Internal backend-facing button feed function is declared
    [x] Internal backend-facing button feed function updates current button state
    [x] Input state can advance one frame by copying current button state into previous state
    [x] isButtonDown reports buttons currently held
    [x] wasButtonPressed reports buttons newly pressed this frame
    [x] wasButtonReleased reports buttons released this frame
    [x] Wrong control-type queries are handled safely
    [x] Public input header includes only the standard headers it needs
    [x] Public input API has /// documentation comments
    Planned input architecture:
    [x] Input owns platform input backends instead of Window interpreting keys long-term
    [x] Add a Win32 input backend under the input system
    [x] Keep Win32-specific input code out of the public engine/input/input.h API
    [x] WindowManager message pump forwards input-related Win32 messages to the input backend
    [x] Win32 input backend registers keyboard and mouse Raw Input devices
    [x] Win32 input backend handles WM_INPUT and translates raw keyboard data into InputState
    [x] Win32 input backend handles WM_INPUT and translates raw mouse data into InputState
    [x] Normal Win32 key messages are kept only as a temporary bridge or menu/text fallback
    [x] Text input is handled separately from gameplay input, e.g. WM_CHAR for typed text
    [x] InputState tracks mouse buttons
    [x] InputState tracks mouse position where useful for UI/menus
    [x] InputState tracks raw mouse movement delta for gameplay camera control
    [x] InputState tracks scroll wheel movement
    [x] Input system can reset/clear transient per-frame input state
    [x] Add input actions above raw keys/buttons
    [x] Add configurable bindings from actions to keys/buttons
    [x] Add XInput backend for controllers
    Agreed public input API shape:
    [x] InputState represents physical device state, not game-specific actions
    [x] InputState stays platform-independent and exposes no Win32/XInput types
    [x] Game code reads input through query functions only
    [x] Backend feed functions use an Internal bridge pattern like Window private message access
    [x] Keyboard, mouse, and controller data share the same frame-advance model
    [x] Actions and bindings will be built above InputState, not inside InputState
    [x] Text input is separate from physical gameplay controls
    [x] Rebinding uses newly activated controls instead of knowing the key/button ahead of time
    Planned core input API:
    [x] Add generic InputDeviceType values: Keyboard, Mouse, Gamepad
    [x] Add generic InputControlType values: Button, Axis, Wheel
    [x] Add InputControl with device type, device index, control type, and numeric control code
    [x] Use physical keyboard scan codes/control codes instead of a giant key enum
    [x] Keep optional named helper constructors/constants for common keyboard controls
    [x] Add helper constructors for keyboard keys, mouse buttons, mouse wheels, gamepad buttons, and gamepad axes
    [x] Add InputActivation for bindable this-frame input changes
    [x] Add activation types for button press/release, axis positive/negative, and wheel positive/negative
    Planned InputState read API:
    [x] advanceFrame copies persistent state and clears per-frame transient state
    [x] clear resets all input state for focus loss/device reset
    [x] isButtonDown reports held button controls
    [x] wasButtonPressed reports newly pressed button controls
    [x] wasButtonReleased reports newly released button controls
    [x] getAxis reports current normalized axis values
    [x] getMouseDeltaX/getMouseDeltaY report current-frame raw mouse movement
    [x] hasMousePosition/getMouseX/getMouseY report the latest known cursor position
    [x] getWheelDelta reports current-frame wheel movement
    [x] isDeviceConnected reports connection state for polled devices such as gamepads
    [x] tryGetFirstActivation returns the first bindable activation this frame
    [x] getActivations copies all bindable activations this frame
    [x] hasTextInput/getTextInputUtf8 expose typed Unicode text for UI/text fields
    [x] InputState stores text input as UTF-8
    [x] InputState encodes Unicode codepoints into UTF-8
    [x] Invalid Unicode codepoints are replaced with U+FFFD
    Planned backend/Internal feed API:
    [x] Internal feed access can set button state
    [x] Internal feed access can set axis values
    [x] Internal feed access can add raw mouse movement delta
    [x] Internal feed access can set or clear absolute mouse position
    [x] Internal feed access can add wheel delta
    [x] Internal feed access can add UTF-8 text or Unicode codepoints
    [x] Internal feed access can set device connection state
    [x] Win32 backend feeds WM_CHAR text as UTF-16 code units
    [x] Win32 backend combines UTF-16 surrogate pairs before feeding text
    [x] Win32 backend handles WM_UNICHAR UTF-32 text when sent
    [x] Win32 backend filters basic control characters out of typed text
    Planned mouse API:
    [x] Add MouseButton identifiers for left, right, middle, and optional extra buttons
    [x] Add MouseWheel identifiers for vertical and horizontal wheels
    [x] InputState stores current and previous mouse button state
    [x] InputState exposes mouse button queries: down, pressed this frame, released this frame
    [x] InputState stores raw mouse movement delta for the current frame
    [x] InputState stores latest absolute mouse X/Y position for UI/menu use
    [x] InputState stores scroll wheel delta for the current frame
    [x] InputState exposes mouse button, position, delta, and wheel queries
    [x] Internal feed access can set mouse buttons, position, movement, and wheel deltas
    [x] Win32 backend feeds mouse buttons from Raw Input
    [x] Win32 backend feeds raw relative mouse movement from Raw Input
    [x] Win32 backend feeds vertical and horizontal mouse wheel movement from Raw Input
    [x] Win32 backend feeds client-area mouse position from WM_MOUSEMOVE
    [x] advanceFrame preserves held mouse buttons and clears transient mouse deltas
    Planned controller API skeleton:
    [x] Add a controller/gamepad slot concept through DeviceIndex
    [x] Add GamepadButton identifiers for common controller buttons
    [x] Add GamepadAxis identifiers for sticks and triggers
    [x] InputState stores controller connected/disconnected state
    [x] InputState stores current and previous controller button state
    [x] InputState stores normalized controller axis values
    [x] InputState exposes controller button queries: down, pressed this frame, released this frame
    [x] InputState exposes controller axis queries for sticks and triggers
    [x] Internal feed access can update controller connection, buttons, and axes
    [x] Add a backend update hook for polled devices such as XInput controllers
    [x] Add Win32/XInput controller backend under the input platform layer
    [x] Apply controller deadzones in the Action layer after the raw state shape is stable
    Migration from previous temporary key-message path:
    [x] Create Win32 input backend files under the input system, e.g. engine/input/platform/win32_input.h and engine/input/platform/win32_input.cpp
    [x] Add the Win32 input backend to CMake
    [x] Move old virtual-key mapping out of the Win32 window implementation and into the Win32 input backend
    [x] Add a backend function that receives Win32 messages and updates InputState
    [x] WindowManager::pollEvents forwards key-related messages to the backend function
    [x] WindowManager::pollEvents contains no virtual-key mapping logic
    [x] Temporary WM_KEYDOWN/WM_KEYUP behavior still works after the backend extraction
    [x] Escape-to-close remains in main/game/action code, not in Window or the backend
    [x] Decide the Raw Input registration API shape, likely a Win32 input function that receives the window HWND as an opaque handle
    [x] Add backend initialization that can register Raw Input devices for the window handle
    [x] WindowManager passes the main HWND to backend initialization without exposing windows.h in public headers
    [x] Switch gameplay keyboard input from WM_KEYDOWN/WM_KEYUP to WM_INPUT
    [x] Keep normal Win32 messages available for menu/text fallback where useful
    [x] Add Action enum for game commands, e.g. Quit, MoveForward, MoveLeft, MoveRight
    [x] Add default action bindings, e.g. Quit -> Escape and MoveForward -> W
    [x] main.cpp uses actions for game behavior instead of hardcoded physical keys
    Current action-layer foundation:
    [x] Action module lives under engine/action with action.h, core/action.cpp, and internal/action.inl
    [x] Action template implementation is split into helper, builder, and ActionMap internal fragments
    [x] Action builds as a static Action library
    [x] Action links against Input while WindowManager remains independent from Action
    [x] ActionMap supports separate maps for separate control plans such as gameplay, UI, and settings
    [x] ActionMap supports configurable button and value bindings
    [x] ActionMap supports multiple bindings per action
    [x] ActionMap supports modifier combos with PrimaryLast and AnyOrder behavior
    [x] ActionMap supports tap, hold, and double-tap triggers
    [x] ActionMap supports Axis1D and Axis2D action values
    [x] ActionMap applies per-binding deadzone, sensitivity, curve, clamp, normalization, and inversion settings
    [x] Action bindings can carry custom settings independently from other bindings on the same action
    [x] Action value evaluation groups compatible same-action/same-device/same-settings bindings before processing paired axes
    [x] ActionMap exposes input snapshot passthrough for text, mouse position, mouse delta, and wheel delta
    [x] Action settings helper functions exist for buttons, axes, movement, mouse look, gamepad sticks, and gamepad triggers
    [x] Action binding validation rejects invalid action, control, binding, and settings data
    [x] InputState exposes a read-only current-button view for rebinding modifier capture
    [x] ActionMap supports capturing and applying rebinding candidates from new input activations
    [x] ActionMap supports stateful multi-key rebinding sessions
    [x] Rebinding sessions can finish when one collected button is released
    [x] Temporary main.cpp rebind test path was removed after Action capture API validation
    Verification:
    [x] Input library compiles without errors
    [x] GameWIP links against Input successfully
    [x] Passing through one frame preserves held buttons
    [x] Pressing a button sets isButtonDown and wasButtonPressed
    [x] Holding a button keeps isButtonDown true and wasButtonPressed false on the next frame
    [x] Releasing a button clears isButtonDown and sets wasButtonReleased
    [x] Releasing an already-up button does not produce a false release event
    [x] Multiple button controls can be tracked independently
    [x] Input behavior is verified from a temporary main.cpp test path
    [x] Escape close behavior works after switching from Key enum queries to generic InputControl queries
    [x] Mouse buttons update InputState from Raw Input
    [x] Raw mouse movement delta updates InputState and clears on advanceFrame
    [x] Mouse wheel delta updates InputState and clears on advanceFrame
    [x] Mouse position updates from WM_MOUSEMOVE
    [x] XInput gamepads update InputState through the Win32 backend update hook
    [x] XInput packet cache does not suppress updates for a different InputState instance
    [x] Keyboard and mouse slot 0 report connected by convention
    [x] WM_CHAR text input appears in InputState as UTF-8
    [x] Unicode text outside ASCII is preserved through InputState
    [x] Basic control keys do not enter the typed-text buffer
    [x] Temporary Quit rebind test captures a keyboard key
    [x] Temporary Quit rebind test captures a keyboard modifier combo
    [x] Temporary Quit rebind test captures an arbitrary multi-key combo
    [x] Temporary Quit rebind test finishes capture when one collected key is released
    [x] Temporary Quit rebind test captures a mouse button
    [x] Temporary Quit rebind test cancels with Escape without closing the window
    [x] Temporary Quit rebind test replaces the old Quit binding
    [x] Temporary action rebind test compile option was removed with the temporary path
    [x] Input test code is removed or guarded after verification
    Optional future polish:
    [ ] Save/load Action bindings from a config file later
    [x] Support multiple bindings per action
    [x] Support analog action values for mouse/controller axes
    [ ] Add input recording/playback only if it becomes useful for debugging

1. MINIMAL RENDERING / DEBUG

[x] Window opens and closes
    Current window foundation:
    [x] Window module lives under engine/window with window.h, core/window.cpp, and platform/win32/win32_window.cpp
    [x] Win32 window implementation is split into platform-private helper/state/access/lifecycle/cursor/mode/monitor fragments
    [x] WindowManager module lives under engine/window_manager with window_manager.h, core/window_manager.cpp, and platform/win32/win32_window_manager.cpp
    [x] Window and WindowManager build as separate static libraries
    [x] WindowManager links against Window and Input
    [x] WindowManager feeds InputState through the Win32 input backend
    [x] WindowManager forwards input-related messages to the input backend instead of owning final input mapping
    [x] Window and WindowManager do not depend on game code
    [x] Window wrapper owns the native window handle
    [x] Window wrapper destroys the native window handle during shutdown
    [x] Win32 window class is registered before window creation
    [x] WM_NCCREATE stores the Window pointer and still allows default Win32 creation handling
    [x] Window creation accepts title, width, and height
    [x] Window title appears from Window::Description title
    [x] Window creation reports failure clearly
    [x] Requested window size is interpreted as client-area size
    [x] Window exposes whether close was requested
    [x] Window exposes a requestClose function
    [x] Window stores current client width and height
    [x] WM_SIZE updates stored client width and height
    [x] Message pump uses non-blocking polling for the future game loop
    [x] WM_CLOSE requests clean shutdown
    [x] WM_DESTROY is handled safely
    [x] Raw Input keyboard packets feed generic InputControl button state
    [x] Win32 keyboard backend uses physical scan/control codes instead of GameWIP::Input::Key
    [x] Temporary WM_SYSKEYDOWN/WM_SYSKEYUP are handled
    [x] Unsupported virtual keys are ignored safely
    [x] Escape key requests window close from game/input-action code, not from Window internals
    [x] Input advanceFrame is called once per loop before processing new messages
    [x] Public window headers avoid exposing windows.h where practical
    [x] Public window API uses GameWIP::Window, GameWIP::WindowManager, and nested Window:: types
    Planned window/control feature set:
    Window completion work order:
    [x] First stabilize the renderer-facing window contract: client size, resize-changed flag, focus state, minimized state, current mode, set mode, set title, request close, and controlled native-handle access
    [x] Then make fullscreen/windowed state restoration reliable before adding more features
    [x] Then split Window::setMode into smaller helpers so each transition path is easier to reason about and verify
    [x] Then add cursor controls: show/hide, confine/unconfine, and an explicit policy for menu mode versus gameplay mode
    [x] Then add Raw Input registration and WM_INPUT handling once the native window handle lifetime is settled
    [x] Verify each step before moving to rendering, because the renderer will depend on window size, focus/minimize state, and native-handle lifetime
    [x] Window::Description supports windowed and borderless fullscreen startup modes
    [x] Window::Description supports resizable/fixed-size configuration
    [x] Window::Description supports requested client-area size
    [x] Window can change title after creation
    [x] Window can change requested/game resolution after creation through setClientSize
    [x] Window keeps requested/game resolution separate from the current dragged client size
    [x] Exclusive fullscreen uses requested/game resolution, not the live dragged client size
    [x] setClientSize behavior while already in fullscreen is defined
    [x] Returning from exclusive fullscreen to windowed applies the current requested/game resolution
    [x] Window can switch between windowed and borderless fullscreen
    [x] Window can switch between all mode pairs: windowed, borderless fullscreen, and exclusive fullscreen
    [x] Window tracks focus/minimized/restored state
    [x] Exclusive fullscreen restores the display mode when focus is lost or the game closes
    [x] Exclusive fullscreen minimizes cleanly on Alt-Tab
    [x] Window can show/hide cursor
    [x] Window can lock/confine cursor when gameplay needs it
    [x] Window supports separate cursor behavior for menu mode and gameplay mode
    [x] Window forwards resize/focus/minimize events for rendering/game systems
    [x] Window exposes a resize-changed flag or event for renderer backbuffer resizing
    [x] Window can expose native Win32 info to the renderer through a controlled Win32-specific API
    Verification:
    [x] Window and WindowManager libraries compile without errors
    [x] GameWIP links against Window and WindowManager successfully
    [x] Window opens with the expected title
    [x] Window opens at the requested outer window size
    [x] Window opens with the requested client-area size
    [x] Stored client size is verified after creation
    [x] Stored client size is verified after manual resize
    [x] setClientSize resizes the windowed client area to the requested size
    [x] setClientSize updates the requested fullscreen/game resolution
    [x] Manual window dragging changes current client size but does not change requested fullscreen/game resolution
    [x] Changing resolution in exclusive fullscreen is preserved when switching back to windowed
    [x] Windowed -> borderless fullscreen transition works
    [x] Borderless fullscreen -> windowed transition works
    [x] Windowed -> exclusive fullscreen transition works
    [x] Exclusive fullscreen -> windowed transition works
    [x] Borderless fullscreen -> exclusive fullscreen transition works
    [x] Exclusive fullscreen -> borderless fullscreen transition works
    [x] Closing while in exclusive fullscreen restores the display mode
    [x] Alt-Tab from exclusive fullscreen restores display settings and minimizes the window
    [x] Window close button exits the game cleanly
    [x] Escape key exits the game cleanly
    [x] W/A/S/D raw keyboard input updates InputState on key down
    [x] W/A/S/D raw keyboard input updates InputState on key up
    [x] Held keys remain down across message pump iterations
    [x] Pressed/released transitions are visible for exactly one frame
    [x] Key-repeat behavior was checked and did not cause false pressed events
    [x] Gameplay keyboard input no longer depends on the temporary key-message path
    [x] Raw Input keyboard path produces the same key state results as the temporary key-message path
    [x] Startup, window creation, close request, and shutdown are logged
    [x] setClientSize after manually maximizing the window restores/redraws the normal window borders correctly
    [x] Release build opens and closes the window without debug-only tests
    Optional future polish:
    [x] Set default Win32 cursor for the client area
    [x] Add high-DPI awareness
    [x] Handle WM_DPICHANGED and use DPI-aware window rect adjustment for client sizing
    [x] Add a renderer-facing Window::Info shape if HWND alone is not enough
    [x] Add a window event path for resize, move, focus, minimize, restore, and close request
    [x] Add a DisplayChanged window event if renderer/UI systems need to react to monitor or display-mode changes
    [x] Switch Win32 title/text calls from A APIs to W APIs with UTF-8 to UTF-16 conversion
    [x] Decide whether Window should explicitly delete move operations or support move construction/assignment
    [x] Add monitor and display-mode query API if fullscreen/monitor selection becomes needed
    [x] Monitor and display-mode query failures report through Window::Result instead of silent empty results
    [x] Add minimum/maximum client-size constraints through WM_GETMINMAXINFO if needed
    [x] Expose current window DPI for future renderer/UI scaling decisions
    [x] Consider replacing bool-only window results with a small Window::Result enum if error handling needs more detail
    [x] Add exclusive fullscreen only if borderless fullscreen is not enough
    [x] Remove temporary WM_PAINT color fill once real rendering clears the frame
    [x] Split Window::setMode into smaller helpers for save/restore/apply mode paths
    [x] Make fullscreen resolution policy explicit in the window API
    [x] Log failures if destroy() cannot restore windowed/display state
    [x] Split applyFullscreen into helpers, so its more easy to read
    [x] Possibly optimize the ring buffer
    [x] Add multiwindow support
        Current multi-window foundation:
        [x] Window::Role exists for MainGame, SecondaryGameView, Tool, and Debug windows
        [x] Window::Description carries a role assigned by game/main.cpp
        [x] Window remains the owner of one native HWND and one event queue
        [x] WindowManager owns multiple Window instances and pumps the Win32 queue once per frame
        [x] WindowManager rejects a second MainGame window
        [x] Raw Input registration moved from Window creation to WindowManager main-window creation
        [x] MainGame is the only role allowed to use exclusive fullscreen
        [x] SecondaryGameView allows windowed and borderless modes only
        [x] Tool and Debug roles are windowed-only
        [x] Cursor confinement is allowed only for MainGame
        [x] Gameplay input routes only to the main window
        [x] Tool input routes only to the focused Tool window when tools are compiled in
        [x] Debug and SecondaryGameView windows do not feed input
        [x] Closing the main window requests game shutdown
        [x] Closing tool/debug windows destroys only those windows
        [x] GAMEWIP_ENABLE_TOOLS controls whether game/main.cpp creates tool-input state and optional startup tool windows
        [x] Startup tool/debug windows are opt-in through openToolWindowsAtStartup
        Build verification:
        [x] Default build compiles with separate Window and WindowManager targets
        [x] Tools-enabled configure/build compiles with GAMEWIP_ENABLE_TOOLS=ON
        Manual verification:
        [x] Default build creates only the main window
        [x] GAMEWIP_ENABLE_TOOLS=ON with openToolWindowsAtStartup=false creates only the main window
        [x] GAMEWIP_ENABLE_TOOLS=ON with openToolWindowsAtStartup=true opens main, tool, and debug windows
        [x] Main-window close exits the game
        [x] Tool/debug close keeps the game running
        [x] Escape requests close on the main window only
        [x] Tool input does not affect gameplay input
        [x] Debug and SecondaryGameView windows do not feed input
        [x] Tool/debug fullscreen requests return OperationNotAllowed
        [x] SecondaryGameView exclusive-fullscreen requests return OperationNotAllowed
        [x] Tool/debug/main cursor confinement policy behaves as expected
        [x] Main fullscreen and cursor behavior still works
        Renderer-era follow-up:
        [ ] Renderer creates one render surface/swapchain per Window
        [ ] Renderer destroys a window surface before WindowManager destroys that Window
        [ ] Renderer recreates each surface independently on that window's resize/DPI changes
        [ ] Renderer skips or throttles minimized/occluded windows per window
        [ ] MainGame and SecondaryGameView windows can each be assigned a camera/view
        [ ] SecondaryGameView window creation is driven from main/game code, not from the platform library
        [ ] Borderless multi-monitor placement is decided in game/editor code using monitor info from Window
        [ ] Tool windows get a UI renderer path before they become interactive editor panels
        [ ] Debug windows get a render-only diagnostics path and remain input-disabled
        [ ] Recheck whether WindowManager needs a pre-destroy callback/event once renderer resources exist

1.5. PRE-RENDER FOUNDATION REVIEW

[ ] Review and harden all foundation code before rendering
    Code quality / cleanup:
    [ ] Review logger, assert, input, and window code end-to-end
    [x] Split oversized Action template internals into focused private include fragments
    [x] Split oversized Win32 window implementation into focused platform-private include fragments
    [x] Remove or guard temporary test code in main.cpp
    [ ] Remove duplicated logic and stale comments
    [ ] Confirm public headers expose only stable engine-facing APIs
    [x] Confirm platform-specific types stay out of public cross-platform headers and internal bridge headers
    [ ] Confirm ownership and lifetime rules are clear for logger/input/window systems
    [ ] Confirm error paths log useful information and leave systems in a valid state
    [x] Move InputStateAccess into a real engine/input/internal header before adding more input backends
    [ ] Decide whether Win32 input backend state should become an explicit object instead of file-local globals
    [ ] Document the shutdown policy: WM_CLOSE requests close, and the game loop owns final window destruction
    [ ] Confirm release build has no debug-only behavior or temporary logs
    [ ] Run normal debug build and release build from VS Code tasks
    [ ] Update roadmap/decisions docs to match the actual architecture
    Optimization / correctness:
    [x] Check logger hot paths for unnecessary allocations after recent changes
    [ ] Check input frame-advance behavior for key-repeat and transient-state correctness
    [ ] Add focused InputState unit tests for press/release, same-frame transitions, clear, text, wheel, axis, and gamepad connection behavior
    [ ] Keep vector-based InputState storage unless profiling shows it matters; consider fixed arrays/bitsets only after a real cost appears
    [ ] Consider exposing input activations as a non-copying view/span if activation copying shows up in profiling
    [x] Apply controller deadzones before using gamepad axes for gameplay bindings
    [ ] Check window message loop for clean shutdown, resize, focus, and minimize behavior
    [ ] Revisit WindowEventQueue complexity; simplify it if maintenance cost outweighs the custom ring-buffer/coalescing behavior
    [ ] Decide Raw Input registration flags policy for foreground/background input and legacy message behavior
    [ ] Check client-area sizing before renderer depends on dimensions
    [x] Check CMake target dependencies and include paths are minimal and clear
    [x] Keep library-specific CMake options/settings in each library's own CMakeLists.txt
    [ ] Check generated release folder only keeps files needed to run the game
    Platform skeleton:
    [x] Add a platform selection layer so game code does not include Win32 headers directly
    [x] Keep Win32 implementation under each module's platform/win32 folder
    [x] Keep future platform folders out of the tree until a backend exists
    [x] Move Window message forwarding access into engine/window/internal
    [x] Move Assert platform bridge into tools/debug/assert/internal
    [x] Add a cross-platform Window API shape that future OS backends can implement
    [x] Add a cross-platform Input backend API shape that future OS backends can implement
    [x] Add CMake conditionals for selecting the platform backend
    [x] Add clear compile-time error for unsupported platforms
    [x] Document that Windows/Win32 is the only supported platform for now
    Verification:
    [x] Clean configure/build still works after cleanup
    [x] Debug/default run opens, logs, receives input, and closes cleanly
    [x] Release run opens and closes cleanly
    [ ] Checklist reflects only actually verified behavior
[ ] Render one object
[ ] Free camera works
[ ] Transform system exists
[ ] Debug draw exists
[ ] On-screen debug info exists
