@page foundation_terminal_unicode_terminal_output Terminal Unicode output

This compatibility page points to the current Terminal Unicode I/O contract.

Windows Unicode behavior is implemented for terminal text input, terminal text output, and redirected byte-oriented streams.

Terminal owns UTF-8 terminal input and output primitives. The current Unicode contract is documented in @ref foundation_terminal_unicode_terminal_io.
